// --- Projects ---

class _ProjectData {
  const _ProjectData({
    required this.title,
    required this.category,
    required this.description,
    required this.tags,
    required this.buttonLabel,
    this.imagePath,
    this.link,
  });
  final String title, category, description, buttonLabel;
  final List<String> tags;
  final String? imagePath, link;
}

const _projects = [
  _ProjectData(
    title: 'AI Resume Reviewer Agent',
    category: 'Generative AI',
    description:
        'An AI-powered resume review agent that compares resumes against job descriptions, '
        'identifies missing skills, generates ATS improvement suggestions, and creates '
        'personalised interview questions.',
    tags: ['Python', 'LangChain', 'RAG', 'Gemini API', 'FastAPI', 'ChromaDB', 'Streamlit'],
    buttonLabel: 'VIEW PROJECT',
  ),
  _ProjectData(
    title: 'Smart Challan',
    category: 'AI + RAG',
    description:
        'An intelligent RAG-based system that retrieves vehicle and traffic violation records '
        'and generates accurate, context-aware challans using semantic search and generative AI.',
    tags: ['Python', 'GenAI', 'RAG', 'LlamaIndex', 'ChromaDB', 'Gemini API', 'FastAPI'],
    buttonLabel: 'VIEW PROJECT',
  ),
  _ProjectData(
    title: 'BayMax',
    category: 'AI Mobile Application',
    description:
        'An AI-powered mobile application built with Flutter and Firebase for symptom tracking, '
        'health monitoring, emergency support, and secure cloud-based health record management.',
    tags: ['Flutter', 'Dart', 'Firebase', 'AI', 'Mobile Development'],
    buttonLabel: 'VIEW PROJECT',
  ),
  _ProjectData(
    title: 'Foodie AI',
    category: 'AI-Powered Food Application',
    description:
        'An AI-powered food discovery and recommendation application designed to help users '
        'discover food based on their preferences, explore suitable options, and make smarter food choices.',
    tags: ['Flutter', 'Dart', 'AI', 'Python', 'API Integration'],
    buttonLabel: 'VIEW PROJECT',
  ),
];

class _ProjectsSection extends StatelessWidget {
  const _ProjectsSection({super.key});
  @override
  Widget build(BuildContext context) {
    final w = MediaQuery.of(context).size.width;
    final mobile = w < 600;
    final gap = SizedBox(height: mobile ? 48 : 80);
    return Padding(
      padding: const EdgeInsets.only(bottom: 96),
      child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
        Text('Featured Projects',
            style: TextStyle(fontSize: _fs(w, 24, 30, 36), letterSpacing: .6)),
        const SizedBox(height: 48),
        _ProjectRow(project: _projects[0], reverse: false),
        gap,
        _ProjectRow(project: _projects[1], reverse: true),
        gap,
        _ProjectRow(project: _projects[2], reverse: false),
        gap,
        _ProjectRow(project: _projects[3], reverse: true),
      ]),
    );
  }
}

class _ProjectRow extends StatelessWidget {
  const _ProjectRow({required this.project, required this.reverse});
  final _ProjectData project;
  final bool reverse;

  @override
  Widget build(BuildContext context) {
    final w = MediaQuery.of(context).size.width;
    final mobile = w < 600;
    final text = _ProjectText(project: project, alignRight: reverse && !mobile);
    final img = _ProjectImg(project: project);
    if (mobile) {
      return Column(crossAxisAlignment: CrossAxisAlignment.start,
          children: [text, const SizedBox(height: 18), img]);
    }
    return Row(crossAxisAlignment: CrossAxisAlignment.center, children: reverse
        ? [Expanded(child: img), const SizedBox(width: 36), Expanded(child: text)]
        : [Expanded(child: text), const SizedBox(width: 36), Expanded(child: img)]);
  }
}

class _ProjectText extends StatelessWidget {
  const _ProjectText({required this.project, required this.alignRight});
  final _ProjectData project;
  final bool alignRight;

  @override
  Widget build(BuildContext context) {
    final w = MediaQuery.of(context).size.width;
    final mobile = w < 600;
    final cross = alignRight ? CrossAxisAlignment.end : CrossAxisAlignment.start;
    final align = alignRight ? TextAlign.right : TextAlign.left;
    final p = project;
    return Column(crossAxisAlignment: cross, children: [
      Text('Featured Project',
          textAlign: align,
          style: TextStyle(
              fontSize: mobile ? 11 : 13,
              color: _violet,
              fontWeight: FontWeight.bold,
              letterSpacing: .5)),
      const SizedBox(height: 6),
      Text(p.title,
          textAlign: align,
          style: TextStyle(
              fontSize: _fs(w, 20, 26, 32),
              color: const Color(0xffd7d5ff),
              fontWeight: FontWeight.w600)),
      const SizedBox(height: 4),
      Text(p.category,
          textAlign: align,
          style: TextStyle(
              fontSize: mobile ? 10 : 12,
              color: _violet,
              letterSpacing: .4)),
      const SizedBox(height: 14),
      Container(
        padding: const EdgeInsets.all(16),
        decoration: BoxDecoration(
            color: const Color(0xee241f42),
            borderRadius: BorderRadius.circular(10),
            border: Border.all(color: const Color(0x335b24a1))),
        child: Text(
          p.description,
          textAlign: align,
          style: TextStyle(
              fontSize: mobile ? 12 : 13,
              height: 1.6,
              color: const Color(0xffd3d1e9)),
        ),
      ),
      const SizedBox(height: 12),
      Wrap(
        alignment: alignRight ? WrapAlignment.end : WrapAlignment.start,
        spacing: 6,
        runSpacing: 6,
        children: p.tags.map((t) => Container(
              padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 3),
              decoration: BoxDecoration(
                color: const Color(0x33a362ff),
                borderRadius: BorderRadius.circular(20),
                border: Border.all(color: const Color(0x66a362ff)),
              ),
              child: Text(t,
                  style: const TextStyle(fontSize: 10, color: Color(0xffd3c7ff))),
            )).toList(),
      ),
      const SizedBox(height: 14),
      OutlinedButton(
        onPressed: p.link != null ? () => launchUrl(Uri.parse(p.link!)) : null,
        style: OutlinedButton.styleFrom(
            minimumSize: const Size(110, 32),
            padding: const EdgeInsets.symmetric(horizontal: 16),
            side: const BorderSide(color: Color(0xff7540ad)),
            shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(8))),
        child: Text(p.buttonLabel,
            style: const TextStyle(fontSize: 11, color: _violet)),
      ),
    ]);
  }
}

class _ProjectImg extends StatefulWidget {
  const _ProjectImg({required this.project});
  final _ProjectData project;
  @override
  State<_ProjectImg> createState() => _ProjectImgState();
}

class _ProjectImgState extends State<_ProjectImg> {
  bool _hover = false;
  @override
  Widget build(BuildContext context) {
    final w = MediaQuery.of(context).size.width;
    final h = w < 600 ? 180.0 : w < 900 ? 200.0 : 230.0;
    return MouseRegion(
      onEnter: (_) => setState(() => _hover = true),
      onExit: (_) => setState(() => _hover = false),
      child: AnimatedContainer(
        duration: const Duration(milliseconds: 200),
        height: h,
        clipBehavior: Clip.antiAlias,
        decoration: BoxDecoration(
          color: const Color(0xff1a0b2e),
          borderRadius: BorderRadius.circular(12),
          border: Border.all(
              color: _hover ? const Color(0xff9b54f1) : const Color(0xff3d1a6e),
              width: _hover ? 1.5 : 1),
          boxShadow: [BoxShadow(
              color: _hover ? const Color(0xbb7127ba) : const Color(0x667127ba),
              blurRadius: _hover ? 56 : 36,
              spreadRadius: _hover ? 4 : 2)],
        ),
        child: widget.project.imagePath != null
            ? Image.asset(widget.project.imagePath!,
                fit: BoxFit.cover, width: double.infinity)
            : _ProjectPlaceholder(title: widget.project.title),
      ),
    );
  }
}

class _ProjectPlaceholder extends StatelessWidget {
  const _ProjectPlaceholder({required this.title});
  final String title;
  @override
  Widget build(BuildContext context) => Container(
        width: double.infinity,
        decoration: const BoxDecoration(
          gradient: LinearGradient(
              begin: Alignment.topLeft,
              end: Alignment.bottomRight,
              colors: [Color(0xff1a0b2e), Color(0xff2d1060), Color(0xff1a0b2e)]),
        ),
        child: Column(mainAxisAlignment: MainAxisAlignment.center, children: [
          Container(
            padding: const EdgeInsets.all(16),
            decoration: BoxDecoration(
                shape: BoxShape.circle,
                border: Border.all(color: const Color(0x66a362ff), width: 1.5)),
            child: const Icon(Icons.code_rounded, color: _violet, size: 32),
          ),
          const SizedBox(height: 12),
          Text(title,
              textAlign: TextAlign.center,
              style: const TextStyle(
                  fontSize: 13,
                  color: Color(0xffe3c7ff),
                  fontWeight: FontWeight.w600)),
          const SizedBox(height: 6),
          const Text('Screenshot coming soon',
              style: TextStyle(fontSize: 10, color: Colors.white30)),
        ]),
      );
}

